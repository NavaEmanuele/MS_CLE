version: 1
mdb_files_glob:
  - "**/*.mdb"
tables:
  # Populate with authoritative CLE_db.mdb table names when confirmed.
  # Example:
  # - name: "scheda_AC"
  #   required_fields: ["ID_AC"]
  # - name: "scheda_AE"
  #   required_fields: ["ID_AE"]
  # - name: "scheda_US"
  #   required_fields: ["ID_US"]
  - name: "scheda_AC"
    required_fields: ["ID_AC"]
relations:
  # Configure real relations once table naming/keys are validated in CLE_db.mdb.
  - from_layer: "CLE/CL_AC.shp"
    from_field: "ID_AC"
    to_table: "scheda_AC"
    to_field: "ID_AC"
    severity_on_missing: "WARN"
